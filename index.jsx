import React from "react";
import ReactDom from "react-dom";

const TestComp = ({})=>{
    return(<div>
        
    </div>)
}

ReactDom.render(<TestComp />, document.getElementById("react-container"));